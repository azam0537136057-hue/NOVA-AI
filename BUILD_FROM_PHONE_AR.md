# بناء NOVA AI من الجوال

المشروع مجهز للبناء السحابي عبر GitHub Actions — لا تحتاج كمبيوتر.

## الخطوات

1. أنشئ مستودعًا جديدًا في GitHub (يمكن أن يكون خاصًا).
2. ارفع **محتويات** مجلد `NOVA_AI` إلى **جذر** المستودع.
   - يجب أن ترى مباشرة: `app/` و `build.gradle.kts` و `.github/` في الجذر.
   - **لا** ترفع المجلد `NOVA_AI` نفسه كطبقة إضافية.
3. افتح تبويب **Actions**.
4. اختر workflow باسم **Build NOVA AI APK** وشغّله يدويًا إن لم يبدأ تلقائيًا.
5. بعد نجاح البناء افتح نتيجة التشغيل ونزّل الـ Artifact:
   **NOVA-AI-debug-apk**

## ملاحظة
هذه نسخة **Debug** للاختبار وليست نسخة Google Play النهائية.
للتثبيت على الجهاز: فعّل «مصادر غير معروفة» ثم ثبّت الـ APK.
