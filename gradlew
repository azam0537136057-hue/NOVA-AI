#!/bin/sh
# Placeholder — استخدم Android Studio أو GitHub Actions للبناء.
# لتشغيل محلي: gradle :app:assembleDebug
echo "استخدم: gradle :app:assembleDebug   أو ارفع المشروع إلى GitHub Actions"
exit 0
